---
import BaseLayout from '../layouts/BaseLayout.astro';
import ResponsiveImage from '../components/ResponsiveImage.astro';
import { getCollection, render } from 'astro:content';

const allPages = await getCollection('pages');
const page = allPages.find((p) => p.id === 'about');
const { Content } = page ? await render(page) : { Content: null };
---

<BaseLayout
	title={page?.data.title ?? 'About Us — Voyage 500'}
	description={page?.data.description ?? 'The family behind the Voyage 500.'}
>

	<section class="page-header">
		<div class="wrap">
			<h1 class="eyebrow page-header__h1">Voyage 500 Catamaran For Sale</h1>
			<p class="page-title">About Us</p>
		</div>
	</section>

	<section class="about-body">
		<div class="wrap about-body__grid">
			<figure class="about-body__photo">
				<ResponsiveImage
					src="/images/about/family.webp"
					alt="Eric, Alex, Leon, and Margot on a rocky shore in the British Virgin Islands"
					sizes="(min-width: 900px) 480px, 100vw"
					loading="eager"
				/>
			</figure>
			<div class="about-body__text prose">
				{Content && <Content />}
			</div>
		</div>
	</section>

	<section class="section section--alt">
		<div class="wrap">
			<p class="eyebrow">Current Position</p>
			<h2>Where we are now</h2>
			<p class="widget-note">Tropicalia's live location via Predictwind.</p>
			<div class="predictwind-widget">
				<iframe src="https://forecast.predictwind.com/tracking/display/SV-Tropicalia/" width="100%" height="500" frameborder="0" loading="lazy" allow="geolocation"></iframe>
			</div>
		</div>
	</section>

	<section class="cta">
		<div class="wrap cta__inner">
			<h2>Want to know more?</h2>
			<p>Happy to answer any questions about our time aboard, the boat's history, or anything else.</p>
			<a href="/contact/" class="btn btn--primary">Get in Touch</a>
		</div>
	</section>
</BaseLayout>

<style>
	.wrap {
		max-width: 1280px;
		margin: 0 auto;
		padding-left: var(--gap-l);
		padding-right: var(--gap-l);
	}

	/* Page header — matches Features, Specs, Gallery, FAQ */
	.page-header {
		padding-top: clamp(80px, 14vw, 120px);
		padding-bottom: var(--gap-l);
		background: var(--color-bg-warm);
	}

	.page-header__h1 {
		display: block;
		margin: 0;
	}

	.page-title {
		font-family: var(--font-display);
		font-size: clamp(2.5rem, 4vw, 3.5rem);
		font-weight: 600;
		color: var(--color-heading);
		line-height: 1.15;
		letter-spacing: -0.01em;
		margin-top: var(--gap-s);
		margin-bottom: var(--gap-s);
	}

	.page-header__sub {
		font-size: var(--text-md);
		color: var(--color-text-muted);
		max-width: 50ch;
		margin: 0;
	}

	/* Body: photo + text */
	.about-body {
		padding: var(--space-3xl) 0;
	}

	.about-body__grid {
		display: grid;
		grid-template-columns: 1fr 1.25fr;
		gap: clamp(var(--gap-l), 6vw, 88px);
		align-items: start;
	}

	.about-body__photo {
		margin: 0;
		position: sticky;
		top: 100px;
	}

	.about-body__photo img {
		width: 100%;
		height: auto;
		display: block;
		border-radius: 6px;
		box-shadow: 0 18px 40px -20px rgba(10, 25, 40, 0.35);
	}

	.about-body__text :global(h2) {
		font-size: clamp(1.6rem, 2.4vw, 2rem);
		font-weight: 600;
		margin: 0 0 var(--gap-m);
		letter-spacing: -0.005em;
	}

	.about-body__text :global(p) {
		font-size: var(--text-md);
		line-height: 1.75;
		color: var(--color-text);
		margin: 0 0 var(--gap-m);
		max-width: 58ch;
	}

	.about-body__text :global(p:last-child) {
		margin-bottom: 0;
	}

	.about-body__text :global(a) {
		color: var(--color-accent);
		text-decoration: underline;
		text-underline-offset: 3px;
		text-decoration-thickness: 1px;
	}

	/* Location widget */
	.section {
		padding: var(--space-3xl) 0;
	}
	.section--alt { background: var(--color-bg-warm); }

	.widget-note {
		color: var(--color-text-muted);
		margin-bottom: var(--gap-m);
	}

	.predictwind-widget {
		overflow: hidden;
		min-height: 400px;
		background: var(--color-bg);
		border-radius: 4px;
	}

	/* CTA */
	.cta {
		background: var(--color-bg-dark);
		padding: var(--size-12) 0;
	}
	.cta__inner { text-align: center; }
	.cta__inner h2 {
		color: #fff;
		font-weight: 600;
		margin-bottom: var(--gap-s);
	}
	.cta__inner p {
		color: rgba(255, 255, 255, 0.5);
		font-size: var(--text-md);
		max-width: 45ch;
		margin: 0 auto var(--gap-m);
	}

	@media (max-width: 900px) {
		.about-body__grid {
			grid-template-columns: 1fr;
			gap: var(--gap-l);
		}
		.about-body__photo {
			position: static;
			max-width: 460px;
			margin: 0 auto;
		}
	}

	@media (max-width: 600px) {
		.wrap {
			padding-left: var(--gap-m);
			padding-right: var(--gap-m);
		}
		.page-header {
			padding-top: 80px;
		}
	}
</style>
